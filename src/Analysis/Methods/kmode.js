// Mixed algorithm i PDF s.9
function consumerClusters(dataIn){
    //let sorted = sortByType();
    //let data = sorted[0];
    //let nrNum = sorted[1]

}


/*
function sortByType(data){
    let i, sortedData, categData;
    let nrNum = 0;
    for(i = 0; i < data.length; i++){
        if(type(data[i][0]) === 'numeric'){
            sortedData.push(data[i]);
            nrNum = nrNum + 1;
        }else{
            categData.push(data[i]);
        }
    }

    for(i = 0; i < categData.length; i++){
        sortedData.push(categData[i])
    }

    return [sortedData, nrNum];
}
*/










export {consumerClusters}